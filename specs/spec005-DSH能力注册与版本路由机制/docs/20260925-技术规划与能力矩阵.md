# DSH 能力注册与版本路由技术规划

日期：2026-09-25

## 1. 当前基线

| 版本 | 事实 | 处理定位 |
| --- | --- | --- |
| DSH `0.1.5-rc.3` | 当前兼容范围下限；部分 Sidebar/终端扩展能力缺失 | 走旧能力适配器和显式降级 |
| DSH `0.1.6-alpha.2` | 当前插件实际依赖和完整验证基线 | 走当前 SettingsScope/旧 Connection/UI 路由 |
| DSH `0.1.7-rc.2` | 截至 2026-09-25 最新可用 0.1.7 线路版本；没有稳定 `0.1.7` | 目标新能力路由 |

当前插件 `0.1.1` 已临时允许安装到 0.1.7，声明范围为：

```text
>=0.1.5-rc.3 <0.1.8-0
```

当前 0.1.7 仅进入实机测试阶段；`version.json` 的实际测试基线仍是 `0.1.6-alpha.2`，完成真实 0.1.7 回放后再宣称正式兼容。

## 2. 能力矩阵

| 能力 ID | 0.1.5-rc.3 | 0.1.6-alpha.2 | 0.1.7-rc.2 | 内部接口 |
| --- | --- | --- | --- | --- |
| `settings.store` | `SettingsProvider`/`SettingsScope` | `SettingsProvider`/`SettingsScope` | `SettingsForms` + `Config`/descriptor | `CodingNsSettingsStore` |
| `connection.rpc` | 旧三参数 handler | 旧三参数 handler | 四参数 handler + `PeerScope` | `CodingNsConnectionRpc` |
| `connection.peer` | 不可用或无 peer 上下文 | 不可用或无 peer 上下文 | `PeerScope`/admission | `CodingNsPeerContext` |
| `ui.icon.plus` | `IconPlusOutline16` | `IconPlusOutline16` | `IconPlusOutlineRegular/Medium` | `CodingNsIconSet.plus` |
| `ui.icon.chevron` | `IconChevronDownOutline14` | `IconChevronDownOutline14` | `IconChevronDownOutlineRegular/Medium` | `CodingNsIconSet.chevron` |
| `locale.runtime` | `LocaleRuntime` 旧 host scope | `LocaleRuntime` 旧 host scope | `ConfigForm` host 参数、异步 apply | `CodingNsLocale` |
| `theme.runtime` | 旧 settings scope | 旧 settings scope | `ConfigForm` host 参数 | `CodingNsTheme` |
| `conversation.tool-call` | 旧 `RunningToolCall` 形状 | 旧 `RunningToolCall` 形状 | `PreparingToolCall`/`StartedToolCall` | `CodingNsConversationTools` |
| `sidebar.right` | 基础 tab API | 基础 tab API | 基础 API 保留，增加焦点/分屏/快捷键 | `CodingNsSidebarRight` |
| `typert.remote` | 单向 Remote/RemoteResult | 单向 Remote/RemoteResult | 新增 Peer/RemoteStream/uplink | `CodingNsTypertRemote` |

矩阵中的“不可用”不是插件整体失败。Feature 只有在其声明了 `required: true` 的能力不可用时才被阻止。

## 3. 现有源码到新架构的映射

### 3.1 现有注册表继续保留

| 现有模块 | 新职责 |
| --- | --- |
| `src/features/registry.ts` | 继续负责 Feature 依赖、启停、资源清理；增加能力门禁调用 |
| `src/shared/contracts/feature.ts` | 增加 `requires`、fallback 和能力诊断字段 |
| `src/client/features/index.ts` | 注册 Client Feature 和它们的能力需求 |
| `src/host/features/index.ts` | 注册 Host Feature 和它们的能力需求 |

### 3.2 需要迁移到能力适配层的代码

| 当前文件 | 当前问题 | 目标适配器 |
| --- | --- | --- |
| `src/host/settings.ts` | 直接使用 `SettingsProvider/SettingsScope` | `host/settings-store` |
| `src/host/rpc.ts` | 直接使用旧 Connection handler/result | `host/connection-rpc` |
| `src/host/features/types.ts` | Host services 暴露 DSH Settings 类型 | `CodingNsCapabilityServices` |
| `src/host/cli-adapters/*` | 多处直接读取 settings scope | `CodingNsSettingsStore` |
| `src/client/index.ts` | 注入和绑定 `settingsScope` | `client/settings-store` |
| `src/client/settings-bridge.ts` | 自建旧 Scope bridge | 旧/新 store adapter，最终删除 bridge |
| `src/client/settings-section.ts` | props 固定为 `SettingsScopeSnapshot` | 内部 `SettingsSnapshot` |
| `src/client/account-bar.ts` | 参数固定为 `SettingsScope` | 内部 settings store |
| `src/client/terminal/*.ts` | 直接读取旧设置类型和图标导出 | settings/icon capability |
| `src/client/external-tool-stream.ts` | 直接依赖 Conversation 旧类型 | conversation adapter |
| `src/client/locale.ts` | 暴露 DSH runtime 具体 Pick | 内部 locale capability |

## 4. 路由优先级规则

同一能力的候选路由按以下顺序选择：

1. DSH 版本范围匹配。
2. 运行时 `detect` 成功。
3. `status: supported` 优先于 `deprecated`。
4. 更高 `priority` 优先。
5. 同优先级仍有多个候选时失败，不随机选择。

版本号只负责初筛，最终选择必须通过实际 Context 能力探测。这样可以处理同一 DSH 版本的不同 Profile 或注入组合。

## 5. 版本与能力的区别

不能把所有版本差异压缩成一个 `isDshVersionAtLeast`：

- DSH 版本表示宿主发行版本。
- 能力 ID 表示某个公开接口是否可用。
- 能力路由表示该接口如何适配。
- Feature 版本表示 Codingns4DSH 自己的业务模块版本。
- Transport/Typert 协议版本表示数据契约版本。

只有能力注册表可以把它们关联起来；业务模块不得把 DSH 版本当作 API 能力本身。

## 6. 旧接口退休账本

每个旧路由都要登记：

| 字段 | 示例 |
| --- | --- |
| 路由 | `settings-scope` |
| 替代路由 | `config-forms` |
| 首次引入 | `0.1.5-rc.3` |
| 弃用版本 | `0.1.7-rc.2` |
| 可移除条件 | 插件最低 DSH 版本 `>=0.1.7`，且三版本矩阵不再要求旧路由 |
| 当前使用者 | `settings.store` adapter |
| 删除证据 | 类型检查、静态引用扫描、三版本测试矩阵 |

删除旧路由前必须更新：

- `version.json`
- `package.json` 的 `engines.dsh` 和 peer dependency
- 所有 DSH 包版本
- 能力矩阵
- Feature 需求声明
- 兼容性报告
- 测试 fixture 和 manifest 测试

## 7. 诊断示例

```json
{
  "dshVersion": "0.1.7-rc.2",
  "capability": "settings.store",
  "routeId": "config-forms",
  "status": "ready",
  "features": ["terminalEnhancement", "cliAdapters"]
}
```

缺失能力示例：

```json
{
  "dshVersion": "0.1.5-rc.3",
  "capability": "connection.peer",
  "status": "unavailable",
  "reason": "当前 DSH Connection 没有 PeerScope",
  "features": ["remotePeerHost"]
}
```

## 8. 开发者扩展标准

新增一个功能模块时，必须按以下顺序：

1. 只选择现有能力 ID；没有对应能力时先新增能力定义和内部接口。
2. 在 Feature descriptor 中写 `requires`，不能写散落的 DSH 版本判断。
3. 通过 `context.services.capabilities.require()` 获取服务。
4. 在能力矩阵中登记支持范围、降级行为和模块使用者。
5. 为至少一个支持版本和一个缺失能力场景增加测试。
6. 更新 Spec 任务和兼容性文档。

禁止事项：

- 在业务 Feature 内新增 `if (dshVersion ...)`。
- 直接导入已经标记 deprecated 的 DSH 类型。
- 通过 `as any` 绕过能力注册表。
- 让一个能力 ID 在不同版本返回不兼容的数据结构。
